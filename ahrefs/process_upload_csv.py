from .models import UrlSearch, Backlinks, GoogleSearch, GoogleBD, CSV_import, ScrapperTest, SingleAhref

from urllib.parse import urlparse
import time
import urllib.request

def upload_0(array, uploaded_file_url, session_id):
    for r in array:
        bk = CSV_import.objects.create(
            document = uploaded_file_url,
            domain = r['domain'],
            backlink = r['url'],
            language = r['lang'],
            site_link = 'Articulo',
            url_to = r['url_to'],
            session_id = session_id )

        print('1 done')


def upload_1(array, uploaded_file_url, session_id):
    for r in array:
        bk = CSV_import.objects.create(
            document = uploaded_file_url,
            domain = r['domain'],
            backlink = r['url'],
            language = r['lang'],
            site_link = 'Articulo',
            url_to = r['url_to'],
            session_id = session_id )

        print('2 done')

def upload_2(array, uploaded_file_url, session_id):
    for r in array:
        bk = CSV_import.objects.create(
            document = uploaded_file_url,
            domain = r['domain'],
            backlink = r['url'],
            language = r['lang'],
            site_link = 'Articulo',
            url_to = r['url_to'],
            session_id = session_id )

        print('3 done')

def upload_3(array, uploaded_file_url, session_id):
    for r in array:
        bk = CSV_import.objects.create(
            document = uploaded_file_url,
            domain = r['domain'],
            backlink = r['url'],
            language = r['lang'],
            site_link = 'Articulo',
            url_to = r['url_to'],
            session_id = session_id )

        print('4 done')


def upload_4(array, uploaded_file_url, session_id):
    for r in array:
        bk = CSV_import.objects.create(
            document = uploaded_file_url,
            domain = r['domain'],
            backlink = r['url'],
            language = r['lang'],
            site_link = 'Articulo',
            url_to = r['url_to'],
            session_id = session_id )

        print('5 done')


def upload_5(array, uploaded_file_url, session_id):
    for r in array:
        bk = CSV_import.objects.create(
            document = uploaded_file_url,
            domain = r['domain'],
            backlink = r['url'],
            language = r['lang'],
            site_link = 'Articulo',
            url_to = r['url_to'],
            session_id = session_id )

        print('6 done')


def upload_6(array, uploaded_file_url, session_id):
    for r in array:
        bk = CSV_import.objects.create(
            document = uploaded_file_url,
            domain = r['domain'],
            backlink = r['url'],
            language = r['lang'],
            site_link = 'Articulo',
            url_to = r['url_to'],
            session_id = session_id )

        print('7 done')

def upload_7(array, uploaded_file_url, session_id):
    for r in array:
        bk = CSV_import.objects.create(
            document = uploaded_file_url,
            domain = r['domain'],
            backlink = r['url'],
            language = r['lang'],
            site_link = 'Articulo',
            url_to = r['url_to'],
            session_id = session_id )

        print('8 done')


def upload_8(array, uploaded_file_url, session_id):
    for r in array:
        bk = CSV_import.objects.create(
            document = uploaded_file_url,
            domain = r['domain'],
            backlink = r['url'],
            language = r['lang'],
            site_link = 'Articulo',
            url_to = r['url_to'],
            session_id = session_id )

        print('9 done')

def upload_9(array, uploaded_file_url, session_id):
    for r in array:
        bk = CSV_import.objects.create(
            document = uploaded_file_url,
            domain = r['domain'],
            backlink = r['url'],
            language = r['lang'],
            site_link = 'Articulo',
            url_to = r['url_to'],
            session_id = session_id )

        print('10 done')

def upload_10(array, uploaded_file_url, session_id):
    for r in array:

        bk = CSV_import.objects.create(
            document = uploaded_file_url,
            domain = r['domain'],
            backlink = r['url'],
            language = r['lang'],
            site_link = 'Articulo',
            url_to = r['url_to'],
            session_id = session_id )

        print('11 done')

def upload_11(array, uploaded_file_url, session_id):
    for r in array:
        bk = CSV_import.objects.create(
            document = uploaded_file_url,
            domain = r['domain'],
            backlink = r['url'],
            language = r['lang'],
            site_link = 'Articulo',
            url_to = r['url_to'],
            session_id = session_id )

        print('12 done')

def upload_12(array, uploaded_file_url, session_id):
    for r in array:
        bk = CSV_import.objects.create(
            document = uploaded_file_url,
            domain = r['domain'],
            backlink = r['url'],
            language = r['lang'],
            site_link = 'Articulo',
            url_to = r['url_to'],
            session_id = session_id )

        print('13 done')

def upload_13(array, uploaded_file_url, session_id):
    for r in array:
        bk = CSV_import.objects.create(
            document = uploaded_file_url,
            domain = r['domain'],
            backlink = r['url'],
            language = r['lang'],
            site_link = 'Articulo',
            url_to = r['url_to'],
            session_id = session_id )

        print('14 done')


def upload_14(array, uploaded_file_url, session_id):
    for r in array:
        bk = CSV_import.objects.create(
            document = uploaded_file_url,
            domain = r['domain'],
            backlink = r['url'],
            language = r['lang'],
            site_link = 'Articulo',
            url_to = r['url_to'],
            session_id = session_id )

        print('15 done')


def upload_15(array, uploaded_file_url, session_id):
    for r in array:
        bk = CSV_import.objects.create(
            document = uploaded_file_url,
            domain = r['domain'],
            backlink = r['url'],
            language = r['lang'],
            site_link = 'Articulo',
            url_to = r['url_to'],
            session_id = session_id )

        print('16 done')


def upload_16(array, uploaded_file_url, session_id):
    for r in array:
        bk = CSV_import.objects.create(
            document = uploaded_file_url,
            domain = r['domain'],
            backlink = r['url'],
            language = r['lang'],
            site_link = 'Articulo',
            url_to = r['url_to'],
            session_id = session_id )

        print('17 done')

def upload_17(array, uploaded_file_url, session_id):
    for r in array:
        bk = CSV_import.objects.create(
            document = uploaded_file_url,
            domain = r['domain'],
            backlink = r['url'],
            language = r['lang'],
            site_link = 'Articulo',
            url_to = r['url_to'],
            session_id = session_id )

        print('18 done')


def upload_18(array, uploaded_file_url, session_id):
    for r in array:
        bk = CSV_import.objects.create(
            document = uploaded_file_url,
            domain = r['domain'],
            backlink = r['url'],
            language = r['lang'],
            site_link = 'Articulo',
            url_to = r['url_to'],
            session_id = session_id )

        print('19 done')

def upload_19(array, uploaded_file_url, session_id):
    for r in array:
        bk = CSV_import.objects.create(
            document = uploaded_file_url,
            domain = r['domain'],
            backlink = r['url'],
            language = r['lang'],
            site_link = 'Articulo',
            url_to = r['url_to'],
            session_id = session_id )

        print('20 done')
